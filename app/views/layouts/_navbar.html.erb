<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand fw-bolder" href="">BugZilla</a>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "text-decoration-none nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Projects", projects_path, class: "text-decoration-none nav-link" %>
        </li>
        <%# <li class="nav-item">
          <%= link_to "Bugs", user_bugs_project_bugs_path(current_user), class: "text-decoration-none nav-link" if user_signed_in? && !current_user.manager? %>
        <%# </li> %>
      </ul>
    </div>
    <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-outline-secondary btn-sm mx-2 text-light" if user_signed_in? %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn-outline-secondary text-light" unless user_signed_in? %>
    <%= link_to "Sign out", destroy_user_session_path, class: "btn btn-outline-secondary btn-sm px-3 text-light" if user_signed_in? %>
  </div>
</nav>
